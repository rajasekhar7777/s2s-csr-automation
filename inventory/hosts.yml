---
# ============================================================================
#  Inventory: structure only.
#  All connection vars live in inventory/group_vars/csr_routers.yml
#  All per-host vars (ansible_host, wan_interface, wan_gateway) live in
#  inventory/host_vars/<hostname>.yml
# ============================================================================

all:
  children:
    csr_routers:
      hosts:
        ZUSE1P2GBSSSVPN001:
        ZUSE1P2GBSSSVPN002:
        ZUSSCGBSPCISCO1:
        ZUSSCGBSPCISCO2:

